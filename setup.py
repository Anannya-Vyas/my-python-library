from setuptools import setup, find_packages

setup(
    name="stattools",
    version="0.1.0",
    description="A lightweight statistics library built from scratch.",
    author="Anannya Vyas",
    packages=find_packages(),
    python_requires=">=3.8",
)
